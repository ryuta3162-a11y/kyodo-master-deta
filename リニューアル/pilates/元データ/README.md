# Pilates 元データ（凍結バックアップ）

**ここが AirReserve 本体の正本です。** 連携用の改修は `../SyncToIdeaPool.gs` など別ファイルで行い、**このフォルダの中身は原則いじりません。**

## 入れるファイル

| ファイル | 内容 |
|----------|------|
| `Code.gs` | Apps Script のメイン（AirReserve Rev.37 など） |
| `Index.html` | Pilates Reformer Dashboard |

## いまの状態

| ファイル | 状態 |
|----------|------|
| `Code.gs` | **未登録** — 下記手順でブックからエクスポートしてください |
| `Index.html` | **未登録** — 同上 |

> **正本はいま Google 側にあります。**  
> 経堂リフォーマーチェック表 → 拡張機能 → Apps Script の内容が動いている本体です。  
> リポジトリに一度取り込み、このフォルダに保存してください。

## エクスポート手順（1回だけ）

1. [経堂リフォーマーチェック表](https://docs.google.com/spreadsheets/d/1Qc1l2pqkoMAgGae_7epfCcvj62a3O_BPbL-ezWDR5pc/edit) を開く
2. **拡張機能 → Apps Script**
3. `Code.gs` を全選択 → コピー → このフォルダの **`Code.gs` に貼り付け（上書き）**
4. `Index.html` も同様に **`Index.html` に貼り付け**
5. 貼ったら **`../Code.gs` と `../Index.html` にも同じ内容をコピー**（作業用）

## 記録（貼り付け後に更新）

| 項目 | 値 |
|------|-----|
| 最終エクスポート日 | （未） |
| バージョン | AirReserve Rev.37（想定） |
| エクスポート担当 | |

## ルール

- **削除・空のプレースホルダーへの置き換え禁止**
- 更新するときは **新しいファイルをここに上書き** → 日付を記録
- 大きな変更の前に Git コミット
