# スタジオ 元データ（凍結バックアップ）

Apps Script の **3ファイル** をそのまま保存します。

| ファイル | Apps Script 上の名前 |
|----------|---------------------|
| `会員データ.gs` | 会員データ.gs |
| `コード.gs` | コード.gs |
| `index.html` | index.html（Git は [Index.html](../Index.html)） |

## 貼り付け先

ホットスタジオ集計ブック → **拡張機能 → Apps Script** → 各ファイルを **同名で** 上書き。
